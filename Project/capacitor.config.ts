import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: ' Ann',
  webDir: 'dist\project\index.html',
  bundledWebRuntime: false
};

export default config;
